---
transport: clustershell
log_file: cumin.log
default_backend: direct
environment: {}
clustershell:
  ssh_options:
    - |
      -o "StrictHostKeyChecking=no"
      -o "UserKnownHostsFile=/dev/null"
      -o "LogLevel=ERROR"
